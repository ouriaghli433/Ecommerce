# Where the browser reaches the Laravel API.
# Nginx serves the backend on port 8080 (see compose.yaml).
VITE_API_URL=http://localhost:8080/api

# Shop name shown in the interface.
VITE_APP_NAME=Verdant
